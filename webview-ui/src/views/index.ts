/**
 * Top-level page views.
 *
 * - MainView.tsx     — Main split layout: sidebar + request builder + response
 * - RequestBuilder.tsx — Request bar, tabs, editors
 * - ResponseView.tsx   — Response area with tabs, error, loading states
 */
