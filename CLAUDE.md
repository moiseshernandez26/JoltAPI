# CLAUDE.md — JoltAPI

This repo's LLM-facing conventions, architecture notes, and gotchas live in **[`AGENTS.md`](./AGENTS.md)** — read that file, not this one, for actual guidance.

This file exists only so tooling that specifically looks for `CLAUDE.md` at the repo root finds something. Keep it thin: put new conventions/gotchas in `AGENTS.md` so there is one source of truth instead of two files drifting apart.

Session-by-session change logs and the full architecture spec live in `docs/` (gitignored, LLM-private memory — see `AGENTS.md` § Documentation Rules for the bootstrap/update rules).
